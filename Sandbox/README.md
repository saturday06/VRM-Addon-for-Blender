# Sandbox (local experiments)

**Local only — not in git.** Throwaway `.blend` files, scripts, and export dumps.

## Do not

- Import sandbox modules from add-on package code
- Commit sandbox files (gitignore blocks them except this README)

## Gitignore

| Path | In git? |
|------|---------|
| `Sandbox/*` | No |
| `Sandbox/README.md` | **Yes** |
